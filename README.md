# Django Custom Model Authentication

- install

    ```bash
    ~$ pip install -r requirements.txt
    ```

- migration

    ```bash
    ~$ python manage.py makemigrations
    ~$ python manage.py migrate
    ```
